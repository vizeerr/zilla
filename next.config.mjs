/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['ipfs.io'],
        unoptimized: true,
      },
      optimizeFonts:false,
}

;

export default nextConfig;
